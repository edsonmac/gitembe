@model IEnumerable<gitembe.Models.tTiempo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TiempoDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TiempoDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TiempoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TiempoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TiempoID })
        </td>
    </tr>
}

</table>
